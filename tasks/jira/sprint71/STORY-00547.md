# STORY-00547: Performance acceptance — 5-friend fog UNION < 3s

**Epic**: E-Friend (Friend System v1) · **Sprint**: 71 · **Points**: 3 · **Owner**: Arch · **Status**: Todo (iPhone-gated)

## Description
Per v4 §14 F5: 5-friend fog UNION wall-clock from Memory-scope toggle to first paint < 3 seconds.

## AC
- [ ] Trace from `setScope('friends')` → first `onFogReady()` fire on iPhone
- [ ] Total time including: API call (`/api/circle/fog`), JSON parse, turf.union of 5 friends + viewer, ShapeSource rebuild, FillLayer first paint
- [ ] Captured via:
  - `performance.now()` markers in code (FogLayer.tsx already logs `fog.union_failed` and ready events)
  - Safari Web Inspector network panel for API latency
- [ ] Record numbers in `_research/friend-system/perf-5-friend-fog.md`
- [ ] If > 3s → optimize candidates: (a) increase SIMPLIFY_TOLERANCE_DEG to 10m, (b) lower ShapeSource.tolerance to 0.5, (c) consider server-side polygon UNION (Fallback B per SPIKE-67-1)

## When
After Story-00546 lands the fog UNION code path. Same iPhone session.
