# STORY-00136: EAS Build + Mapbox SDK Setup

**Epic**: E-001 Map
**Sprint**: Sprint 42
**Sprint Goal**: Phase 1 Foundation — Real Mapbox + GPS precision
**Points**: 3
**Owner**: DevOps + Frontend Dev
**Status**: Todo

## Description
As a developer, I want to configure EAS development build and install @rnmapbox/maps so that native Mapbox rendering works on physical devices.

## Expected Result
`eas build --profile development` produces a working dev client with Mapbox rendering capability. App can display a basic Mapbox map centered on NZ.

## Acceptance Criteria
- [ ] `@rnmapbox/maps` installed and configured in app.json plugins
- [ ] `expo-blur` installed for glassmorphism effects
- [ ] `react-native-reanimated` installed for spring animations
- [ ] `eas.json` created with development profile
- [ ] Mapbox access token configured (environment variable, not hardcoded)
- [ ] EAS build succeeds for iOS (or Android if iOS unavailable)
- [ ] Basic MapView renders centered on Tongariro region (-175.6, -39.2)

## Dependencies
- Requires Mapbox account + access token
- Blocks: STORY-00137, STORY-00138, STORY-00139

## Notes
- Mapbox free tier: 25K MAU
- Token must be in .env or EAS secrets, never committed
- This breaks Expo Go compatibility — all future testing via dev client
