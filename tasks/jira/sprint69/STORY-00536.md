# STORY-00536: Trails Activities stays Mine-only (no Friends sub-tab)

**Epic**: E-Friend (Friend System v1)
**Sprint**: Sprint 69
**Sprint Goal**: Extend visibility model to Routes + Trails
**Points**: 2
**Owner**: Frontend + UX
**Status**: Done

## Description

v4 plan §10 binding: Trails Activities tab is **first-person only**. NO Mine|Friends sub-tab. The other two trails tabs (Flags + Routes) get the sub-tab; Activities does not. Story exists to make the omission explicit (and to flag if any prior change accidentally added one).

## Expected Result

`RoutesScreen.tsx` Activities tab renders without a Mine|Friends sub-tab control. If a sub-tab control is found, remove it.

## Acceptance Criteria

- [ ] Inspect ActivitiesTab component in RoutesScreen.tsx
- [ ] Confirm NO Mine|Friends sub-tab control is present
- [ ] If present, remove it
- [ ] Activities tab list shows only the viewer's own activity history
- [ ] No call to `/api/circle/*` from ActivitiesTab
- [ ] Static visual sanity: tab toggle row at the top still shows Activities|Routes|Flags only

## Dependencies

- None (purely defensive verification)

## Notes

This Story is small but explicit so the v4 §10 binding is documented in the Sprint trail. Skipping it would risk a later PR silently adding a sub-tab to Activities.

---

## Execution Report (2026-06-27)

### Status: Done — defensive verification

### Inspection result
ActivitiesTab function at `app/src/screens/RoutesScreen.tsx:563` confirmed clean:
- FilterSortBar with filters [all, hiking, running] + sorts [recent, distance-desc, duration-desc]
- NO Mine|Friends scope sub-tab
- NO `/api/circle/*` call

### Verification
- Code inspection complete (no edits needed)
- Playwright visual confirmation: `docs/qa/sprint69-evidence/STORY-00536-activities-no-subtab.png` shows Activities tab with empty state + no scope sub-tab
