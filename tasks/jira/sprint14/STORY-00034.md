# STORY-00034: SessionStore — completed route history persistence

**Epic**: E-002 地图与导航
**Sprint**: Sprint 14
**Sprint Goal**: HikingScreen 真实 GPS + 本地标记持久化
**Points**: 2
**Owner**: Frontend Dev
**Status**: Todo

## Description
As a hiker, I want completed hiking sessions saved locally so that MapHistoryScreen can show my real route history in Sprint 16.

## Expected Result
- `useSessionStore` Zustand slice persisting completed sessions
- When tracking stops, session auto-saved with all stats + track points
- Sessions retrievable by regionCode for future MapHistoryScreen integration

## Acceptance Criteria
- [ ] `src/store/useSessionStore.ts` exists
- [ ] `TrackingSession` type exported (reused by RunningScreen in S15)
- [ ] `addSession(session)` persists to storage key `cairn_sessions`
- [ ] `getSessions()` returns all sessions sorted by startedAt desc
- [ ] `getSessionsByRegion(regionCode)` returns filtered sessions
- [ ] After stop tracking in HikingScreen: session appears in store immediately
- [ ] Sessions survive page refresh (storage persistence verified)

## Dependencies
- Depends on: STORY-00033 (geo.ts types needed)

## Notes
Storage: same localStorage wrapper. Max 100 sessions stored (oldest pruned).
TrackPoints array may be large — consider storing only summary in main record, trackPoints separately.
