# Sprint 51 Goal

**Phase 3 Start: AR foundation + Community data model + marker proximity filter**

Set up AR framework (@viro-community/react-viro), create AR scene component with GPS-anchored markers, build community store for shared markers, and implement marker proximity/density filtering.
