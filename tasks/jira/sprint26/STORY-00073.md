# STORY-00073: UX fixes bundle — Sprint 25 medium items

**Epic**: E-006 UI/UX
**Sprint**: Sprint 26
**Sprint Goal**: Flag planting excellence + SettingsScreen premium uplift
**Points**: 2
**Owner**: Frontend Dev
**Status**: Done

## Description
As a Cairn user, I want small UX confusions resolved, so that the app feels polished and intentional throughout.

## Expected Result
Three targeted fixes from Sprint 25 UX review: (1) Run Complete "New Run" button no longer shows a back-chevron icon; (2) MapHistory expanded capsule shows "No GPS" for distance (matching the collapsed card); (3) warning triangle on map placeholder has a label so first-time users understand it.

## Acceptance Criteria
- [ ] Run Complete "New Run" button: icon changed from ChevronRight to ArrowRight (forward direction), or icon removed entirely — no backward-pointing chevron
- [ ] MapHistory expanded capsule km stat: when session has no GPS distance, capsule shows "No GPS" label (same text as the collapsed card secondary line) instead of "--"
- [ ] HikingScreen map placeholder: warning triangle (red circle with exclamation) has a text label "Offline" or "No map data" directly below it, FontSize.small, Colors.textMuted — purpose is now clear to new users
- [ ] All three fixes verified at 390px viewport with zero console errors
- [ ] No regressions on Run Complete screen layout, MapHistory accordion, or HikingScreen map

## Dependencies
- None (3 independent targeted fixes, bundled per micro-story rule)

## Notes
Sprint 25 UX review items: Run Complete chevron (Low), MapHistory capsule "--" (Medium), warning triangle unlabeled (Low). Medium item is the priority. All three are ≤0.5 pts each — bundled per micro-story bundling rule.
