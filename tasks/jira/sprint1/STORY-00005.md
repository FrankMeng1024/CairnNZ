# STORY-00005: Spike — 三层测试路径搭建与验证

**Epic**: E-001 基础架构
**Sprint**: Sprint 1
**Sprint Goal**: 验证所有系统级依赖的技术可行性
**Points**: 2
**Owner**: Frontend Dev
**Status**: Done

## Description
As a developer, I want a smooth testing pipeline that lets me self-verify without a device, and lets the user accept by simply opening Expo Go, so that there's no gap between "passes in dev" and "works on device".

## Expected Result
`npm test` 在 Windows 上无设备运行通过；Expo Go 扫码可直接验收功能，无需额外步骤。

## Acceptance Criteria
- [ ] `npm test` 在 Windows 本机零设备运行通过（Jest + RNTL 环境搭建成功）
- [ ] `expo-location` mock 返回真实 LocationObject 结构（签名与官方 API 一致）
- [ ] `expo-speech` mock 可被 Jest spy，验证调用参数
- [ ] 至少一个示例组件测试通过（RNTL render + fireEvent）
- [ ] `expo start --web` + Playwright 可截图 UI 布局（Layer 2 已验证）
- [ ] `docs/spike-results/SPIKE-005.md` 记录三层路径结论

## Dependencies
- 无（纯 JS 环境，不依赖其他 Spike）

## Notes
Spike结论写入 `docs/spike-results/SPIKE-005.md`

**2026-05-15 Spike 结论**: VIABLE — 8/8 测试通过
- Jest@29 + jest-expo@55 + RNTL@13 环境搭建成功
- expo-location mock：签名与官方 API 完全对齐（LocationObject 结构、NZ 坐标）
- expo-speech mock：speak/stop/getAvailableVoices 均可 spy，onStart/onDone 回调验证通过
- 纯函数（haversine）测试：无 mock 依赖
- `npm test` 运行时间：~7s（Windows，无设备）
- Validation Warning：`setupFilesAfterFramework` 为已知 jest-expo@55 警告，不影响测试执行
