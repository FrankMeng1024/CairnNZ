# STORY-00147: Expo Web Rendering Verification

**Epic**: E-001
**Sprint**: Sprint 44
**Points**: 3
**Owner**: QA Lead
**Status**: Todo

## Description
Verify the app renders correctly in Expo web mode. All new components (GlassPanel, MapBottomPanel, GPSStatusBar) must degrade gracefully on web since Mapbox native won't render there.

## Acceptance Criteria
- [ ] `npm start -- --web` starts without crashes
- [ ] HomeScreen renders correctly (no errors)
- [ ] MapScreen shows fallback UI ("Build with EAS to enable Mapbox")
- [ ] HikingScreen shows fallback UI  
- [ ] RunningScreen renders timer and controls
- [ ] GlassPanel renders with CSS backdrop-filter on web
- [ ] No uncaught exceptions in browser console
- [ ] Playwright screenshot of each main screen captured as evidence
- [ ] All navigation paths work (tab switching, screen push/pop)

## Dependencies
- All Sprint 42-44 code

## Notes
- Mapbox won't render on web (native-only), but app must not crash
- This validates graceful degradation and web-compatible styling
