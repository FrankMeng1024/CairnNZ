# STORY-00528: Implement 8 new backend endpoints with permission filters

**Epic**: E-Friend (Friend System v1)
**Sprint**: Sprint 67
**Sprint Goal**: Establish schema, backend, data foundation, and Mapbox fog UNION feasibility
**Points**: 5
**Owner**: Backend
**Status**: Done

## Description

As Cairn backend engineer, I need to implement 8 new endpoints for Friend System v1: Memory subscriptions, circle (markers/routes/fog UNION), public marks bbox query, and hide-from-view. Plus harden POST/PATCH on markers + routes to reject `permission='public'` from client.

## Expected Result

8 new endpoints live, contract-tested, return correct data per v4 plan §7. 4 "public rejection" contract tests pass.

## Acceptance Criteria

- [ ] `POST /api/memory-subscriptions` body `{friend_id}` — triggers DB cap check + friend-must-be-friend check; returns 201 or 409 (cap) or 403 (not friend)
- [ ] `DELETE /api/memory-subscriptions/:friend_id` — removes subscription
- [ ] `GET /api/memory-subscriptions` — returns my subscribed friends list
- [ ] `GET /api/circle/markers` — returns UNION of subscribed friends' Friend+Public markers (LEFT JOIN hidden_items, filter where viewer has hidden it)
- [ ] `GET /api/circle/routes` — same for routes
- [ ] `GET /api/circle/fog` — server-side polygon UNION of subscribed friends' memory_points (or precomputed fog geometry)
- [ ] `GET /api/markers/public?bbox=lat1,lng1,lat2,lng2` — strangers' public marks in bbox, ORDER BY created_at DESC LIMIT 50
- [ ] `POST /api/hide` body `{item_type, item_id}` — INSERT IGNORE INTO hidden_items
- [ ] **v4 H1 enforcement**: POST /api/markers + PATCH /api/markers/:id reject `permission='public'` → 400
- [ ] **v4 H1 enforcement**: POST /api/routes + PATCH /api/routes/:id reject `permission='public'` → 400
- [ ] All 4 "public rejection" cases have explicit contract tests
- [ ] All endpoints use `backend/src/constants/permission.js` for ENUM values

## Dependencies

- Blocks: F2 Sprint 68 (frontend consumes these endpoints)
- Depends on: STORY-00525 (schema), STORY-00527 (test data)

## Notes

CLAUDE.md compliance: every endpoint needs at least one happy-path + one error-path test (Layer 2 functional test). Concurrent INSERT test for memory-subscriptions trigger is per STORY-00525 already.

Reference: `_research/friend-system/FINAL_PRODUCT_PLAN_v4.md §7 + §1 row L`.

---

## Execution Report (2026-06-27)

### Status: Done

### Files delivered
- `backend/src/routes/memory-subscriptions.js` — POST/DELETE/GET, maps trigger SIGNAL → HTTP (403 not-friend, 409 cap, 409 dup)
- `backend/src/routes/circle.js` — GET /markers, /routes, /fog. Uses subscribed-friend list; LEFT JOIN hidden_items; normalizes legacy `group` → `friend` on wire; anonymizes Public marks/routes (author_name=null).
- `backend/src/routes/hide.js` — POST /; validates type, validates item exists, rejects hiding own item, idempotent via ON DUPLICATE KEY UPDATE.
- `backend/src/routes/markers.js` — added GET /public?bbox= (50-row, ORDER BY DESC, anonymized, hidden-filtered, bbox-validated). Hardened POST/PUT against permission='public' (v4 H1). Removed unreachable `public_snapshot` branch.
- `backend/src/routes/routes.js` — Hardened POST/PUT against permission='public' (v4 H1).
- `backend/src/models/Route.js` — Now persists `permission` ('personal'|'friend') on create/update; SELECT also returns permission (Arch review Medium fix).
- `backend/src/index.js` — Registered 3 new route mounts.
- `backend/scripts/seed/integration_test_story_528_serverside.sh` — 23-check integration test.

### Test results (23/23 PASS)
T01 Alice login · T02 Bob login · T03 sub-before-friend→403 · T04 friend insert · T05 sub→201 · T06 dup→409 · T07 list (count=1, limit=5) · T08 circle/markers=8 · T09 circle/routes=1 · T10 circle/fog groups=1 · T10b fog points=3 · T11 public bbox=18 (DB-computed) · T12 hide carol→201 · T13 public after hide=17 · T14 POST mark public→400 · T15 PUT mark public→400 · T16 POST route public→400 · T17 PUT route public→400 · T18 DELETE sub→200 · T19 new route permission persists='friend' · T20 circle/routes=2 after add · T21 personal NOT visible in circle.

### Arch review (subagent verdict)
**PASS** with 1 Medium + 1 Low. Both addressed in this Sprint:
- **Medium FIXED** — Route model now persists permission (was previously discarding the field). Verified by T19/T20/T21.
- **Low FIXED** — Bob seeded with 3 memory_points; T10 + T10b now exercise the JSON_ARRAYAGG path with real data.

### Spec deltas (acceptable per Arch)
1. Story AC text says PATCH; implementation gates the actual PUT verb (routes.js + markers.js never had PATCH). H1 security intent fully met on the real write path.
2. circle/fog returns raw memory_points grouped by friend_id; server-side polygon UNION deferred per SPIKE-67-1 outcome (v4 §7 explicitly allows "or precomputed fog geometry").
3. Hide uses `ON DUPLICATE KEY UPDATE` instead of literal `INSERT IGNORE`; functionally identical with cleaner 201/200 distinction.

### Deployment notes
- All new routes copied into `cairn-backend` container `/app/src/`.
- index.js patched in-container via sed (the container has additional routes not in local repo: edit-diag, edit-envelope, feature-events — preserved untouched).
- Container restart confirmed; health returns 200.
- Persistence reminder: container changes survive restart but not rebuild. Future image rebuild must include this commit's source.
