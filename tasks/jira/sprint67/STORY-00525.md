# STORY-00525: Apply migration 018 + permission constant centralization

**Epic**: E-Friend (Friend System v1)
**Sprint**: Sprint 67
**Sprint Goal**: Establish schema, backend, data foundation, and Mapbox fog UNION feasibility
**Points**: 3
**Owner**: DBA + Backend
**Status**: Done

## Description

As Cairn DBA, I need to apply migration 018 (Friend System v4 schema) to aliyun MySQL — adding users columns, routes.permission, memory_subscriptions table with race-safe trigger, hidden_items table. As Backend engineer, I centralize the permission ENUM legacy normalization (`'group'` → `'friend'` UI-layer) into one constants file.

## Expected Result

Migration 018 applied to prod. New tables/columns visible. Trigger fires correctly under concurrent load test. `backend/src/constants/permission.js` exports a single source of truth for ENUM values + normalize helper.

## Acceptance Criteria

- [ ] Write `backend/migrations/018_friend_system_v4.sql` per v4 plan §6 (verbatim)
- [ ] Backup aliyun MySQL with mysqldump before applying
- [ ] Apply migration; verify all 4 new tables/columns exist: `users.account_type`, `users.memory_subscription_limit`, `routes.permission`, `memory_subscriptions`, `hidden_items`
- [ ] Verify trigger `trg_memory_subscription_cap` exists and uses `SELECT ... FOR UPDATE`
- [ ] Concurrent insert test: 2 parallel INSERT to memory_subscriptions at limit-1 → exactly 1 succeeds (race safety)
- [ ] Friend-must-be-friend trigger check: INSERT memory_subscriptions for non-friend pair → rejected
- [ ] Create `backend/src/constants/permission.js` with `PERMISSION`, `SHARED_VISIBILITY`, `normalize()` exports
- [ ] All existing backend code that references `'group'` literal updated to import from constants

## Dependencies

- Blocks: STORY-00527 (seed needs users columns), STORY-00528 (endpoints need tables)
- Depends on: none

## Notes

Migration is additive only (zero destructive ALTER). Legacy `'group'` ENUM value stays in DB unchanged.

Reference: `_research/friend-system/FINAL_PRODUCT_PLAN_v4.md §6 + §1 rows L/M/N/S`.

## Execution Report (2026-06-27)

**Files created**:
- `backend/src/migrations/018_friend_system_v4.sql` — applied to aliyun MySQL prod
- `backend/src/constants/permission.js` — single source of truth for ENUM normalization

**Migration applied 2026-06-27 19:15 to aliyun cairn DB**

**Verification (SHOW COLUMNS / SHOW TABLES / SHOW TRIGGERS confirmed)**:
- ✅ `users.account_type` ENUM('free','pro') DEFAULT 'free'
- ✅ `users.memory_subscription_limit` INT DEFAULT 5
- ✅ `routes.permission` ENUM('personal','friend','public') DEFAULT 'personal'
- ✅ `memory_subscriptions` table created (FK + indexes)
- ✅ `hidden_items` table created (FK + index, no FK on item_id by design)
- ✅ `trg_memory_subscription_cap` trigger active (SELECT FOR UPDATE + friend-must-be-friend check)

**Backup**: `backup/pre-friend-system-20260627_191304.sql` (29MB)

**MySQL syntax compatibility**: Initial v1 used `ADD COLUMN IF NOT EXISTS` (MariaDB-only). Rewrote with information_schema conditional ALTER for MySQL 8.0 compatibility. Idempotent.

**Notes**:
- `markers.permission` ENUM unchanged (still has 'group' legacy value). App layer normalizes to 'friend' on read via `permission.js::normalize()`.
- No destructive operations; pure additive schema.

**No further work needed.**
