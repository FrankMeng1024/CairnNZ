# STORY-00527: Seed 9 mock @cairn.demo accounts with single-char passwords

**Epic**: E-Friend (Friend System v1)
**Sprint**: Sprint 67
**Sprint Goal**: Establish schema, backend, data foundation, and Mapbox fog UNION feasibility
**Points**: 2
**Owner**: Backend
**Status**: Done

## Description

As Cairn backend engineer, I need to seed 9 mock user accounts (`1@cairn.demo` ~ `6@cairn.demo`, `x1@cairn.demo` ~ `x3@cairn.demo`) with bcrypt-hashed single-character passwords (`1`, `2`, ..., `x3`) directly into the database, bypassing the auth.js register flow.

## Expected Result

9 mock accounts exist in `users` table. Each can login via `POST /api/auth/login` with the documented password. Their initial data state matches v4 plan §8 (Alice has 3 sessions+12 marks+1 route; Dave is empty; Stranger 1 has 1 Public mark; etc).

## Acceptance Criteria

- [ ] Write `backend/scripts/seed/gen_bcrypt_hashes.js` that produces bcrypt hashes for passwords `1`, `2`, `3`, `4`, `5`, `6`, `x1`, `x2`, `x3` (cost factor matches auth.js register, e.g. 12)
- [ ] Write `backend/scripts/seed/seed_mock_users.sql` with 9 INSERT statements
- [ ] Write `backend/scripts/seed/seed_mock_sessions_markers_routes.sql` for each account per v4 plan §8 expected data
- [ ] All GPS coordinates in mock data fall within 9163's activity bbox (run bbox query first, embed result)
- [ ] Stranger 1 mark must be within 50m of 9163 Back Loop path (geometric verification SQL)
- [ ] Carol initial state: NOT a friend of 9163 yet (no row in `friends` table)
- [ ] All 9 mock accounts login test: `POST /api/auth/login {email,password}` → 200 + JWT
- [ ] Write `backend/scripts/seed/backup_mock_data.sh` — mysqldump WHERE `email LIKE '%@cairn.demo'`
- [ ] Write `backend/scripts/seed/restore_mock_data.sh` — load latest snapshot
- [ ] Write `backend/scripts/seed/clear_mock_data.sql` — DELETE WHERE `email LIKE '%@cairn.demo'`, never touches 9163

## Dependencies

- Blocks: F2 Sprint 68 (UI needs real friends data)
- Depends on: STORY-00524 (login works for short pw), STORY-00525 (schema in place), STORY-00526 (9163 bbox known)

## Notes

User拍板：no `is_mock` flag column. `email LIKE '%@cairn.demo'` is the only mock filter. Production must hard-assert `users WHERE email LIKE '%@cairn.demo'` returns 0 rows on startup (server-side block).

Reference: `_research/friend-system/FINAL_PRODUCT_PLAN_v4.md §8 + §1 row V + §17.4`.

---

## Execution Report (2026-06-27)

### Status: Done

### Files delivered
- `backend/scripts/seed/gen_bcrypt_hashes.js` — bcrypt cost 12 (matches `User.hashPassword`), prints JSON of 9 hashes
- `backend/scripts/seed/seed_mock_users.sql` — 9 INSERT IGNORE rows with baked hashes
- `backend/scripts/seed/seed_mock_sessions_markers_routes.sql` — sessions/markers/routes per §8, with built-in geometric verification + per-user counts
- `backend/scripts/seed/backup_mock_data.sh` — mysqldump WHERE `email LIKE '%@cairn.demo'` + child tables via mock user ids
- `backend/scripts/seed/restore_mock_data.sh` — loads latest snapshot from `backups/`
- `backend/scripts/seed/clear_mock_data.sql` — child-first cascade DELETE with hard guards (refuses if `id=4` ever ends up in the set, refuses if any non-`@cairn.demo` row is mixed in)

### Aliyun apply results
- Mock users seeded: 9 (verified `SELECT COUNT(*) FROM users WHERE email LIKE '%@cairn.demo'` = 9)
- Per-user data counts match v4 plan §8 exactly:
  - Alice 3/12/1, Bob 2/8/1, Carol 2/4/0, Dave 0/0/0, LDY 4/15/2, Eve 2/6/0, x1 0/1/0, x2 0/3/0, x3 0/5/0
- Geometric verification: Stranger 1 mark at `(31.232248, 121.434262)` → distance to 9163 Back Loop center = **20.02m** (< 50m ✓)
- Login test: all 9 accounts return HTTP 200 + JWT via `POST /api/auth/login`
- 9163 (user_id=4): sessions=1, markers=0, routes=0, memory_points=46, memory_subscriptions=0 — unchanged from Story-526

### Permission ENUM mapping (note for Arch + Story-528)
- `markers.permission` is legacy ENUM `('personal','group','public')` — `'group'` represents the "Friend" tier. Seed inserts use `'group'` for friend-visible markers; `permission.js` already handles `group ↔ friend` normalization on read/write.
- `routes.permission` is ENUM `('personal','friend','public')` (migration 018) — seed uses `'friend'` directly.

### Outstanding finding (out of scope for Story-527 — flag to user)
- `friends` table contains 10 pre-existing rows for `user_id=4` from 2026-05-19, NOT created by this seed. They link 9163 to legacy real users (`816354835@qq.com`, `test@example.com`, `testverify@example.com`, `devtest_final@test.com`, `ldy@sina.com`).
- RESUME.md / v4 §8 requires "9163 initial state = 0 friends, 0 subscriptions" for the test journey, but these are real-user friendships not mock data.
- Per `feedback_dry_run_before_delete`: I will NOT silently delete these. User decision needed before F2/F3 testing begins.
- `memory_subscriptions` for user_id=4 is 0 ✓ (no action needed).
