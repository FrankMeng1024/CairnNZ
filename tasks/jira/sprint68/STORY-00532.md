# STORY-00532: Detail Sheet 4 forms (§4.11 A/B/C/D)

**Epic**: E-Friend (Friend System v1)
**Sprint**: Sprint 68
**Sprint Goal**: Land the Mark surface for Friend System v1.
**Points**: 5
**Owner**: Frontend + UX
**Status**: Done

## Description

Tap on a mark opens the Mark Detail Sheet. Per v4 §4.11 the sheet has **4 distinct forms**, selected by the (creator × visited × visibility) tuple per v4 §3 three iron laws.

## Expected Result

Tap any visible mark → sheet renders the correct form. Tap a "far + outside fog" mark = no sheet (form D = silent). Action surface respects iron laws.

## Acceptance Criteria

- [ ] Iron law 1 wired: `visible(mark) = in_my_fog(mark) OR (in_subscribed_friend_fog AND visibility_grants_me)`. Personal-from-friend → not visible (NO sheet, see scenario §9 #13).
- [ ] **Form A** (mine, any visibility): shows tier icon header + Edit + Delete (true DELETE). Like/Report appears only if visibility='public' (per §4.11 actual rule). Edit opens existing mark-edit flow.
- [ ] **Form B** (other-mark + I visited): tier icon + author_name (only if Friend tier — Public tier anonymized per v4 row Q) + visited badge ✓ + Like 💛 + Report 🚩 + Delete (from-view).
- [ ] **Form C** (other-mark + friend's fog + NOT visited): tier icon + author_name (Friend tier only) + helper text "(Walk here to like/report)" + Delete-from-view ONLY.
- [ ] **Form D**: tap = no-op. Mark renders as dim gray icon (covered by Story 531 stranger-style). No sheet, no toast, no haptic.
- [ ] Header tier label uses locked icons: 🔒 Personal / 👥 Friend / 🌍 Public (Public only appears when displaying a public mark — never user-creatable)
- [ ] `author_name` field rendered ONLY for Friend tier marks. Public marks display NO name regardless of creator's friend status (v4 row Q).
- [ ] Close button (✕) restores map without re-running any data query
- [ ] All 6 scenarios from v4 §9 scenarios 8/10/11/12/13/14 produce the correct form (Carol Public, LDY Friend visited, LDY Friend not-visited, own Public, friend Personal hidden, stranger blurred)
- [ ] Static visual fidelity: render each form at 375×812 viewport (iPhone target), screenshot, side-by-side against the §4.11 ASCII mockups — no visual deviations beyond Cairn token mapping
- [ ] Empty / null text content handled (some marks have no `text` field)

## Dependencies

- Blocks: STORY-00533 (Like/Report + Delete actions plug into this sheet)
- Depends on: STORY-00530 (permission ENUM) + STORY-00531 (visual tokens)

## Notes

- Iron-law 1 implementation lives in `app/src/features/marks/utils/markVisibility.ts` — pure function `getVisibility(viewer, mark, fog)`. Reused by map render (Story 531) and sheet (this Story).
- `in_my_fog` already computed by existing FogLayer pipeline for the viewer's own corridors. Reuse: don't recompute.

Reference: `_research/friend-system/FINAL_PRODUCT_PLAN_v4.md §3 iron laws + §4.11 forms + §9 scenarios 8-14`.

---

## Execution Report (2026-06-27)

### Status: Done

### Files delivered
- **NEW** `app/src/features/marks/utils/markVisibility.ts` — pure `getMarkVisibility()` (iron law 1) + `getMarkDetailForm()` returning A/B/C/D
- **NEW** `app/src/features/marks/components/MarkDetailSheet.tsx` — 1 modal rendering all 4 forms by switching on the form value; props for onClose/onEdit/onDelete/onLike/onReport/isLiked. Public marks anonymized (author_name=null) regardless of friend status.
- **NEW** `app/src/features/marks/dev/MarkDetailDevPreviewScreen.tsx` — dev-only route with 7 scenarios covering forms A (personal + public) / B (friend + stranger public) / C / D (friend personal + far stranger) for visual verification
- **MODIFIED** `app/src/store/useMarkerStore.ts` — added optional `authorName` field to Marker
- **MODIFIED** `app/src/navigation/RootNavigator.tsx` — registered MarkDetailDevPreview route
- **MODIFIED** `app/src/screens/HomeScreen.tsx` — `__DEV__`-gated entry link to dev preview
- **MODIFIED** `app/src/screens/MapScreen.tsx` — tap on a marker opens MarkDetailSheet first (form selection); Edit button inside form A then falls through to existing EditMarkerSheet

### v4 plan compliance
- §3 iron law 1 enforced: `visible(mark) = in_my_fog OR (in_subscribed_friend_fog AND visibility_grants_me)`; friend's Personal mark hidden (no sheet, no tap response — form D).
- §4.11 form A: Edit + Delete (+ Like/Report iff Public)
- §4.11 form B: tier badge + author_name (Friend tier only) + visited ✓ + Like + Report + Delete-from-view
- §4.11 form C: tier badge + author_name (Friend tier) + "(Walk here to like/report)" + Delete-from-view only
- §4.11 form D: returns null (no sheet)
- v4 row Q: Public marks anonymized client-side even when creator is a friend — verified visually in dev preview "B. Stranger Public" scenario

### Verification
- **9/9 logic tests PASS** via `app/src/features/marks/__tests__/markVisibility.dev-test.mjs`:
  - own in fog, own not in fog → form A
  - friend friend-tier + visited → form B
  - friend friend-tier + not visited + subscribed → form C
  - friend personal (blocked by visibility_grants_me) → form D
  - stranger public not in fog → form D
  - stranger public + visited → form B
  - friend legacy 'group' + visited → form B
  - friend tier not subscribed not visited → form D
- **Playwright visual screenshots** (`docs/qa/sprint68-evidence/`):
  - STORY-00532-formA-personal-mobile.png — Form A Personal (Edit + Delete, no Like/Report)
  - STORY-00532-formA-public.png — Form A Public (Edit + Delete + Like + Report)
  - STORY-00532-formB-friend.png — Form B Friend visited (LDY author + visited ✓ + Like + Report + Hide)
  - STORY-00532-formB-public-anon.png — Form B Public ANONYMOUS (NO author name)
  - STORY-00532-formC-friend-fog.png — Form C (author + walk-to-interact helper + Hide only)
- Type-check clean (no new errors; baseline HikingScreen 'AR' nav unchanged).

### Deferred (per `feedback_unity_visual_test`)
- Live device verification on iPhone — logged for F5 hardening Sprint
- Wiring `loadCircleMarkers()` so subscribed-friend marks actually appear on the Map (then tap→sheet exercises forms B/C with real data, not dev preview)
