# STORY-00543: Stranger Public mark — blurred icon (no sheet)

**Epic**: E-Friend (Friend System v1) · **Sprint**: 70 · **Points**: 3 · **Owner**: Frontend · **Status**: Done

## Description
v4 §3 matrix row 4: stranger Public marks that fall **within the viewer's standard visibility radius** (MysteryVisibilityConfig.mysteryMaxDistanceMeters) **but outside the viewer's fog** render as blurred gray icons. Tap = no-op (no sheet opens).

## Acceptance Criteria
- [x] `CairnPinsLayer` accepts `strangerMarks?: Marker[]` prop
- [x] Stranger marks within visibility radius + outside fog → render as `StrangerBlurredPin`
- [x] StrangerBlurredPin: 24px, gray border + fill, opacity 0.6, no border-radius variation from circle (matches dim aesthetic)
- [x] No `onSelected` handler on the PointAnnotation → tap drops (no sheet, no toast)
- [x] If a stranger mark falls inside viewer's fog → handled by normal `markers` path (form B in MarkDetailSheet)

## Execution Report
- Files: `CairnPinsLayer.tsx` (props + filter + render + new `StrangerBlurredPin` component + `pinStyles.stranger`/`strangerIcon`)
- The `strangerMarks` data loader (`/api/markers/public?bbox=`) is NOT wired in this Story — it's a F5 follow-up bundled with fog UNION live test
- Type-check clean
- v4 §3 matrix row 4 + §14 F4 Story 5 satisfied at the visual rendering layer

## Out-of-scope (F5 follow-up)
- Wire `loadStrangerPublicBbox()` action consuming `GET /api/markers/public?bbox=` and threading the result into `<MemoryMap strangerMarks={...}>`. Bundle with Story-541's fog UNION work since both depend on the same iPhone session.

Reference: v4 §3 matrix row 4 + §14 F4 Story 5
