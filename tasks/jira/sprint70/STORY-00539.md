# STORY-00539: Memory tab Mine|Friends toggle

**Epic**: E-Friend (Friend System v1) · **Sprint**: 70 · **Points**: 3 · **Owner**: Frontend · **Status**: Done

## Description
Add a Mine|Friends scope toggle to the Memory tab top bar. Drives FogLayer + MarkLayer behavior (Stories 541, 543).

## Acceptance Criteria
- [x] New store `useMemoryScopeStore` (scope: 'mine' | 'friends')
- [x] New component `MemoryScopeToggle` rendered in `MemoryScreen` top bar next to BackButton
- [x] Toggle is pill-shaped with 2 segments; default 'mine'
- [x] Visual style matches Cairn sepia tokens

## Execution Report
- Files: `useMemoryScopeStore.ts`, `MemoryScopeToggle.tsx`, `MemoryScreen.tsx` (import + render + top bar style)
- Top bar `justifyContent: 'space-between'` so BackButton + toggle balance
- Type-check clean
- Playwright deferred — Memory tab requires real Mapbox token for any meaningful render; web verification scope-toggle component logic only

Reference: v4 §10 + §14 F4 Story 1
