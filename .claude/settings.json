{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "printf '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"REMINDER: Face problems directly — when current path (e.g. Unity) hits errors, diagnose root cause first, never propose retreating to rejected alternatives (Viro is permanently dead — do not suggest it, no exceptions). Switching approach requires: (1) root cause proven unfixable on current path, (2) user-initiated discussion, (3) explicit AskUserQuestion. If you catch yourself thinking maybe try X instead as a way to avoid current complexity — stop, go back to the actual problem.\"}}'",
            "shell": "bash"
          }
        ]
      }
    ]
  }
}
