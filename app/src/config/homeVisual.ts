/**
 * Home visual feature switches.
 *
 * Gate A1.2 keeps the approved Sunny environment static while its source
 * resolution and runtime rendering are reviewed on a physical phone. The
 * motion implementation and assets remain intact for later review.
 */
export const SUNNY_AMBIENT_MOTION_ENABLED = false;
