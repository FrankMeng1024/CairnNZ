// Stub — no SVG assets for Auth (all PNG). Kept so generated component's
// import path resolves.
import type React from 'react';
export const svgByAsset: Record<string, React.ComponentType<{ size?: number; color?: string }>> = {};
