// Stub — no SVG assets for Running (all PNG). Kept so generated component's
// import path resolves.
export const svgByAsset: Record<string, React.ComponentType<any>> = {};
import type React from 'react';
