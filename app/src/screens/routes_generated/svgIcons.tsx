// Stub — no SVG assets for Routes (all PNG or text glyphs). Kept so generated
// component's import path resolves.
import type React from 'react';
export const svgByAsset: Record<string, React.ComponentType<any>> = {};
