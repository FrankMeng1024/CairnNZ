# Cairn V1 closure control

Run: `20260918T222838+0800`

## CURRENT CLOSEOUT — OTA_CANDIDATE_READY / FORMAL RELEASE HOLD — 2026-09-21T21:32:00+08:00

The current source/test fingerprint is
`46aa7e1af18fb8187512991bd3a3d3450f892eebd2bb01867a03c0cd8d534040`
across 664 files. The immutable FINAL PF R8 run completed once with exit `0`,
matching preflight/final source fences, PASS gate/final/operator receipts,
ordinary Personal and Friends loaded-Web journeys, real isolated API/MySQL,
and complete owned-resource cleanup. Its 82 bound dependencies remain exact
after the isolated O61 marker/test and closeout-tool changes. Its R8 console contract records 42
request-time deliberate offline aborts, 42 exact console candidates, 42
one-to-one correlations, zero unmatched events, and keeps N01's POST/404
evidence separate.

Public R15 remains PASS through its exact 30-file dependency ledger. The
current-lineage focused regression remains PASS at 23 files / 255 tests and is
supplemental rather than a replacement for connected evidence. R4 preflight,
R5 source-fence, and R7 delayed-console attempts remain formal FAIL history
and are not allocated as PASS.

Formal release closure remains **HOLD**:

- the controlling Activity/Memory performance result remains
  `tile_geometry=162 ms` against the unchanged `<150 ms` ceiling;
- FR-DISCOVERY r5.1 remains
  `FAIL_HOLD_FORMAL_PLAN_EVIDENCE_LIMITATION` after its one allowed N=2001
  attempt;
- no deployment or OTA was performed, and native/physical GPS/field evidence
  remains unverified.

The distinct, fail-closed owner-test gate is **`OTA_CANDIDATE_READY`**. The
owner's convergence contract expressly permits the retained performance-
deferred and plan-evidence/P2 limitations because neither is a known P0. It
requires all 20 functional assertions PASS, PF R8/Public R15/C3 dependency
equivalence, no known P0 or pending local functional blocker, loaded Homepage
evidence, the loaded O61 Auth capture, and preserved no-deployment/Public-off
boundaries. O61 is visible in the isolated 390×844 loaded page; no OTA was
published.

Machine-readable authority is generated from `05_EVIDENCE_INDEX.json` into
`FINAL_RUN_REGISTRY.json`, then evaluated into `CLOSEOUT_VALIDATION.json`.
Narrative text cannot manufacture PASS or override the controlling HOLDs.
The generated registry is
`sha256:9f010ecec71ac6606270eb1e2ac9bcefce014c058cc698cf41da483f07bee5d6`;
the validation is
`sha256:14f87e65e6c15c53795dc7e03fda6bbcbbd31c956ca1f080eec96ecac4ba372e`
with integrity PASS, 20/20 assertions PASS, controlling states `[PASS,HOLD]`,
deployment `NO_DEPLOYMENT`, formal result `HOLD`, and separately derived
`OTA_CANDIDATE_READY=true`.

## EXECUTION_PACKET_RECONCILED — 2026-09-20T19:07:46+08:00

The live closure posture is **HOLD**. No stage is complete and this metadata
reconciliation does not authorize deployment, OTA, native claims, cleanup, or
an unchanged retry. The current product fingerprint is
`bcd168db8240d24302e6559451d0f0e1af5d2957d17b651bc177524b6d6ae698`
(646 files); HEAD remains `393338b5c159e7341d22b94086b5c29455386bdf`.

- F1/F2 has a fixture-backed Expo Web **PASS** at fingerprint `fa986cc8…` for
  the connected normal-entry Hike/Run journey. Activity, Cairn, live-before-
  Finish Memory, Finish, Route edit/reload, and Use Route identity assertions
  passed. This is not real API/MySQL, real Mapbox, native mounted Mapbox,
  physical GPS, field, or responsiveness evidence. The Web Route presentation
  truth was the exact `Map unavailable` fallback with
  `plannedRouteSourceLoaded=false`; native Mapbox source evidence is NOT_RUN.
  Receipt: `evidence/f1/fa986cc808e067b98563d9cf72fa6cd885d038d414fafe342d98a9f701b1bff5/F1_F2_CONNECTED_PASS.md`
  (`sha256:d6a34edfd2ca4ebfbe3d753fea313ea139e24171a973a1840c3cf78ecc8f59ce`).
- A4 remains **FAIL/HOLD**. The controlling gate measured the labelled
  `tile_geometry` slice at 162 ms against the unchanged `<150 ms` ceiling;
  123/124 suites and 1,170/1,174 tests passed, which does not erase that
  failure. The attempted phase/allocation diagnostic is
  **DEFERRED_ENVIRONMENT/HARNESS / UNVERIFIED**. No threshold, radius, truth,
  or Finish timing was weakened.
- FR-DISCOVERY-500 v2 is integrated in the current fingerprint, but the real
  isolated MySQL 8.0.36 r2 N=2001 plan gate is **FAIL/HOLD**. N=10000 and the
  E/F API/MySQL run are NOT_RUN. The external r4 evidence-preserving runner is
  pending independent static review and has not been run against MySQL.
- The current C1/C2 focused cache/authorization suite passes 23/23, but the C1
  full gate remains **OPEN**: the durable invalidation R1 candidate is not
  integrated because independent account-switch counterexamples showed stale
  block/unfriend completions can purge or present across owners. Durable
  storage truth and initiating-viewer/generation fencing must land together.
- Auth/Settings R13 is **BLOCKED / NOT INTEGRATED** by unfenced Feedback draft,
  acknowledgement, haptic, and owner-switch behavior. Public authority R2 is
  likewise **BLOCKED / NOT INTEGRATED**; Public remains disabled by default.
- F7-1 Public Detail v4 is focused GREEN only in its external candidate and is
  pending independent review/integration. Homepage F5 v3 passed all ten
  fixture-backed programmatic Web assertions, while owner visual acceptance
  and native review remain pending. F8 Phase 1 inventory is complete; no
  cleanup, move, deletion, database mutation, or archive action was performed.

Truthful next work is independent review of the frozen r4, C1 R2, and F7-1 v4
packages; correction of the blocked F6/Public account fences; and a separately
justified A4 gate. The retained failures remain controlling until those exact
dependencies produce new evidence.

## FUNCTIONAL_RUN_READY — 2026-09-20T10:16:31+08:00

The owner disabled the custom Stop hook and cleared the old supervision Goal.
Stale owner-pause and RECOVER_ONLY runtime intent is cleared; no automatic T08
or canary drill remains. Functional work may resume under the ordinary one-
writer and one-heavy-workload controls, but no implementation was started in
this metadata-recovery turn.

T08 remains **PASS** on its existing receipts: real XHigh lanes, explicit owner
pause, quiescent checkpoint, RECOVER_ONLY reconciliation, same-root native Goal
resume, and useful resumed work under the preserved lane identity. The broader
unattended Stop guard is **FAIL / DISABLED_FOR_NOW** because stale pause/
RECOVER_ONLY state caused the gate to block again after the owner had resumed
and left the run unattended. The historical hook definition and receipts are
retained; no hook rebuild was performed.

Product fingerprint remains
`cab465fa6ff734ac5ad6b1c5f13e2076c13cf350b26207dae9b05e009768a604`
(636 files). The old Goal is absent, all previous workers are quiescent, no
owned functional process is live, and no migration/deployment outcome is
unknown. The isolated review MySQL service remains idle on `127.0.0.1:3310`.
Fast remains OFF.

## RECOVERY_READY — 2026-09-20T00:20:11+08:00

RECOVER_ONLY is active. All three current XHigh lane generations are
checkpointed and quiescent, no owned process or shared mutation has an unknown
outcome, and no feature work may be scheduled. After the original RECOVER_ONLY
turn and two automatic continuations, the three-turn blocked-audit threshold
was satisfied and the native Goal was marked `blocked` at
`2026-09-20T00:37:53+08:00`. No feature work was resumed. The Stop gate allows
this intentional return; use native `/goal resume` only when ready.

Current product fingerprint is
`cab465fa6ff734ac5ad6b1c5f13e2076c13cf350b26207dae9b05e009768a604`
(636 files). Pre-final-receipt recovery tree
`e35b1c389671e411174d49e90aa0253204a5b6b5` is stored through alternate index
`/Users/mzm/Desktop/cairn_revision03_work/baseline/recover-only-20260920T001700p0800.index`.
See `supervision/RECOVER_ONLY_CHECKPOINT.json` for identities, hashes, partial
results, services, and exact resume tasks.

T08 is PASS because useful same-root post-recovery work was observed. The
subsequent canary is INTERRUPTED/NOT PASS: the owner requested RECOVER_ONLY
before the no-owner-prompt window closed, and the observer recorded 18
UNCERTAIN samples. Its terminal log/report are preserved in
`supervision/CANARY_REPORT.json`; a fresh window starts only after the next
verified native Goal resume. T01/T04 were not rerun, Fast remains OFF, and A4
remains FAIL/OPEN at 180 ms against the frozen 150 ms ceiling.

## SUPERVISION_VERIFIED; CANARY30 active — 2026-09-19T23:46:03+08:00

T08 passed after the same-root native Goal resumed and surviving Lane C created
a new filesystem-timestamped, exact-fingerprint acceptance-delta artifact. No
agent ID or writer was duplicated. The 30-minute no-owner-prompt observation
window runs through at least `2026-09-20T00:16:03+08:00` while the three lanes
continue productive approved work. Lane A's first resume receipt contained an
impossible old timestamp and was rejected pending an explicit correction; it
was not used to pass T08.

## Native Goal resume observed — 2026-09-19T23:41:20+08:00

The same root Goal is active. Root/session identity, product fingerprint, hook
hashes, ordinary Git index, child identities, process ownership, and retained
service state all match the RECOVER_ONLY checkpoint. The supervision mode is
now RUN and follow-ups are being dispatched to the same three XHigh lane IDs;
no replacement writer is permitted. T08 remains incomplete until a useful
resumed artifact is observed, after which the 30-minute no-owner-prompt canary
starts while productive approved work continues.

## RECOVER_ONLY ready — 2026-09-19T23:33:16+08:00

**RECOVERY_READY.** The live T08 pause checkpoint was reconciled in the same
root session without resuming feature work. The native Goal is `paused`, Fast
remains OFF, and the explicit owner pause remains authoritative. T08 is still
not PASS: it requires a later native Goal resume and an observed useful result
from a surviving lane.

- Product fingerprint remains
  `044045c3ad1ecde3c8eb9c55b76a72c14dfc46a39d8c14bf659b51e498e29929`
  across 633 files. Comparison with the T08 pause tree found only supervision
  metadata drift; no product/test source or evidence artifact drifted.
- `/root/rev03_a4_resume` and `/root/functional_verification_lane` are
  completed/checkpointed and reusable by follow-up. `/root/homepage_material_lane`
  was interrupted only after its checkpoint; its identity and scratch survive.
  No duplicate lane was created.
- Sessions `46877`, `90650`, and `84048` are terminal. No owned writer, test,
  browser, backend, migration, or deployment process is active. The isolated
  review MySQL service remains deliberately idle on `127.0.0.1:3310`; the
  unrelated builder MySQL remains untouched.
- The 180 ms Memory slice against the frozen 150 ms ceiling remains FAIL/OPEN.
  The later profile is diagnostic only. Lane C's open normal-caller findings
  and Homepage's zero-candidate partial state remain unchanged.
- Existing live T01/T04 evidence was reused; neither drill was rerun. Hook
  definition, handler, and queue-adapter hashes still match their preparation
  receipts, and the active-runtime loading proof remains the existing T01
  invocation for this unchanged root session.

On the next native Goal resume, first recheck the Goal/root identity, agents,
owned processes, product fingerprint, and hook hashes. Then clear the pause,
enter RUN, and reuse the same three lane IDs. Mark T08 PASS only after a lane
acknowledges and produces useful resumed work; immediately begin the 30-minute
no-owner-prompt canary while approved work continues. Do not rerun T01 or T04.

## T08 owner-live pause checkpoint ready — 2026-09-19T23:14:00+08:00

**PAUSED_CHECKPOINT_READY.** The owner explicitly requested the live T08 pause.
All three Sol XHigh lanes returned bounded checkpoints. Lane A and Lane C are
terminal/quiescent; Lane B reported no live tool/process and was then
interrupted to enforce quiescence. No owned test, browser, server, profiler,
migration, or deployment remains active. The isolated review MySQL container
on `127.0.0.1:3310` remains deliberately idle and the unrelated builder MySQL
container remains untouched.

- Exact product fingerprint remains
  `044045c3ad1ecde3c8eb9c55b76a72c14dfc46a39d8c14bf659b51e498e29929`
  across 633 files.
- `verify:changed` is FAIL, not PASS: 109/120 suites and 1,124/1,154 tests
  passed. The 10k Memory slice observation of 180 ms against frozen `<150 ms`
  remains **FAIL/OPEN**. A later profiled diagnostic pass does not erase it.
- Lane A changed only `app/__tests__/useAppStore.test.ts` (+7/-9) to match the
  O41 single Memory-authority contract; focused authority tests passed 11/11.
- Lane C's 41/41 baseline is retained with explicit normal-path gaps for the
  friend-circle account fence, friend Cairn Hide, Settings account/single-
  flight guards, and Public disabled/capability races. None is fixed yet.
- Homepage controls and measurements are preserved at
  `/Users/mzm/Desktop/cairn_revision03_work/homepage_candidates/lane-b-material-20260919-01/`;
  no candidate was generated and owner visual acceptance remains UNVERIFIED.
- T08 is not PASS until a later `RECOVER_ONLY` reconciles these exact lane and
  process identities and native Goal resume is separately observed. No work
  may auto-resume before then.

## Hard overnight functional closure active — 2026-09-19T22:45:00+08:00

The owner activated
`/Users/mzm/Desktop/CairnNZ_overnight_functional_closure_master_01.md`
(`sha256:dd435fac7fe71f5d556d3b4896f54d7a75e2b33af49c8688682bc8a13a0dcd09`)
for this existing Goal and v04 supervision system. The permission-explicit pause
is cleared; the native Goal is active. Fast remains OFF.

- Parent `/root` remains Sol Ultra for orchestration/integration only.
- `/root/rev03_a4_resume` is the sole primary-tree writer and sole heavy-local-
  workload owner for Revision-03 A4.
- `/root/homepage_material_lane` advances isolated Homepage material work.
- `/root/functional_verification_lane` advances read-only Friends, Settings,
  and dependency-independent Public verification.
- T08 remains pending. Prepare its bounded checkpoint as soon as two lanes have
  real IDs, acknowledged scope, and useful first progress; PASS requires the
  later explicit owner pause, RECOVER_ONLY reconciliation, and native Goal
  resume.
- Release infrastructure, hosting migration, billing, provider consoles,
  recruitment, App Store work, OTA publication, and general Public enablement
  remain excluded.

## Explicit owner pause — permission-explicit restart — 2026-09-19T21:58:04+08:00

**OWNER_PAUSED — DO NOT AUTO-RESUME.** This is a temporary owner pause for a
permission-explicit restart, not completion, cancellation, or a software HOLD.
The project Stop hook must allow this pause and the Goal must not restart work.
Resume only after a new explicit owner instruction in the replacement root
session.

- Active stage: Revision-03 `A4`, exact-source validation; Public `B4` remains
  preserved and gated behind the Revision-03 software gate.
- Exact product fingerprint: `044045c3ad1ecde3c8eb9c55b76a72c14dfc46a39d8c14bf659b51e498e29929`
  (633 files); candidate remains unpublished `O60`, runtime `0.2.6`.
- Final loaded Raw GPS evidence for that exact fingerprint passed 24/24 with
  zero runtime errors at `evidence/a4/044045c3ad1ecde3c8eb9c55b76a72c14dfc46a39d8c14bf659b51e498e29929/loaded-web/manifest.json`.
- The required `npm run verify:changed` was then interrupted on the owner's
  instruction (exec session `9895`, exit `130`) before producing a result. Its
  receipt is `evidence/a4/044045c3ad1ecde3c8eb9c55b76a72c14dfc46a39d8c14bf659b51e498e29929/verify-changed-interrupted.json`.
- All three child lanes are completed/quiescent. The final verifier found no
  concrete A4 blocker in its partial exact-fingerprint review, but its full
  assertion-by-assertion audit remains unfinished.
- Expo/Metro and all Cairn test runners/backends are stopped. The isolated
  closure MySQL container on `127.0.0.1:3310` is deliberately retained; the
  unrelated builder MySQL container is untouched.
- Recovery index:
  `/Users/mzm/Desktop/cairn_revision03_work/baseline/owner-pause-restart-20260919T215804p0800.index`.
  Pre-checkpoint recovery tree: `4addb72cc99d0da8aa47d2110510abc59716af97`;
  final checkpoint recovery tree: `aef6a7f73bac71e9c327943cc7cd662c7634d7a5`.
- First work after explicit resume: rerun the interrupted changed-Activity gate
  from the beginning, finish the paused verifier audit, then decide A4 without
  repeating the already-passing loaded-Web journey unchanged.

## Explicit owner pause — 2026-09-19T17:49:35+08:00

**OWNER_PAUSED — DO NOT AUTO-RESUME.** This is a temporary owner pause, not
completion or cancellation. No Stop hook or unattended worker may restart the
mission. Root session `01a0b4c1-b498-76e0-95bd-1976d11586f2`; source
fingerprint `9cee885d048b37ba1257f2651e0add6aa0a37b583e59e5404cb8df2636722632`.
The complete safe-boundary record, live-process inventory, evidence paths, and
resume action are in `OWNER_PAUSE_CHECKPOINT.md`.

This run continues Personal/Friends revision 03 and then adds the controlled,
text-only Public Cairn pilot. It does not restart or replace the accepted
Personal/Friends contract.

## Checkpoint

- Active stage: **OWNER PAUSE during exact-source validation/freeze**.
- Next action after explicit owner resume: diagnose the loaded Raw GPS
  accepted-evidence-to-Memory timing failure (2026.4 ms vs frozen 40 ms)
  without an unchanged retry, then complete fingerprint-dependent final gates.
- Repository HEAD at continuation: `393338b5c159e7341d22b94086b5c29455386bdf`.
- Complete dirty-tree snapshot at continuation: Git tree
  `efaae41a06104af272b4b9b772d9c5e107a2e6bb`, alternate index
  `/Users/mzm/Desktop/cairn_revision03_work/baseline/closure-start.index`.
- Original revision-03 task-start tree: `f40b740bddbf2c67cf9362512cf945c38f0965a8`.
- Candidate marker/runtime: unpublished `O60`, app/runtime `0.2.6`.
- Current additive schema target: migration `042` (server-owned source realm,
  canonical Activity snapshot, source segments, immutable Public review
  snapshots) on top of migration 041's server-gated Public pilot.
- One writer: the primary agent in this working tree.

## Stage-A checkpoint — 2026-09-19T00:29:32+08:00

- Isolated local API: `http://127.0.0.1:3010` (loopback only).
- Isolated local database: container `cairn-v1-closure-mysql`, schema
  `cairn_v1_closure`, MySQL `8.0.36`; no unrelated database was touched.
- Real API/MySQL run: 17/17 grouped assertions passed in
  `/Users/mzm/Desktop/cairn_revision03_work/evidence/local-api-mysql-rev03g.json`.
- The run includes the exact C4 old-observation/recent-pair counterexample and
  a post-grant finalized-Activity revisit without coverage timestamp mutation.
- A same-second Cairn edit exposed a real content-revision collision. Migration
  040 plus monotonic writer increments repaired it; the sensitive failed run
  remains in `local-api-mysql-rev03f.json`.

## Continuation checkpoint — 2026-09-19T01:23:49+08:00

- Final clean Personal/Friends isolated API/MySQL run: 18/18 assertions in
  `/Users/mzm/Desktop/cairn_revision03_work/evidence/local-api-mysql-rev03-final6.json`.
  This adds explicit server-side Public-disabled containment.
- Activity changed gate: 513/513. Public shared cache races: 10/10, including
  offline relaunch without lease renewal and durable presentation retry.
- Public migration 041, exact-revision moderation, bounded encounter/discovery,
  interactions, shared cache, author UI, read-only Detail, and protected
  operator command are implemented. The earlier clean Public API/MySQL run is
  13/13; an expanded final rerun is active after temporal and retry repairs.
- Loaded PF failed runs remain attributable. The latest two exposed and then
  validated the Route-sheet closure fix; the only current rerun interruption
  was Expo hot reload while source was changing, so the final loaded run is
  deliberately deferred until source settles.

## Realistic-Memory checkpoint — 2026-09-19T02:26:15+08:00

- Internal Expo Web was rebuilt with both a valid public Mapbox token and the
  explicit QA-only Activity Simulator capability. Runs missing either build
  input are retained as environment failures and are not accepted evidence.
- Loaded O55 Raw GPS seed 550101 passed 12/12 stage assertions in
  `/Users/mzm/Desktop/cairn_revision03_work/evidence/raw-gps-loaded-final5`:
  live Fog contained nine isolated footprints before Finish, stayed at nine
  during 90 simulated stationary seconds, reached 20 after resumed movement,
  stayed at 20 through Finish, and reloaded as a distinct runtime/object.
- Synthetic point provenance now remains explicitly `simulator_test` in the
  separate durable QA realm. Personal coverage and presence both remained
  zero; focused provenance/pipeline tests passed 12/12.
- A 60-second real API/MySQL endurance sensitivity run passed 30 cycles and
  198 requests after two retained workload defects were corrected. The
  required 30-minute final workload is active in isolated run
  `v1end_final01`.

## Reviewer-correction checkpoint — 2026-09-19T16:35:00+08:00

- Both bounded read-only reviewers completed and returned HOLD findings; no
  worker remains in flight. Their concrete findings are recorded in
  `06_REVIEW_FINDINGS.md` and are being dispositioned by the primary writer.
- Fresh disposable MySQL 8/API runs after the canonical-evidence SQL repair
  passed 18/18 Personal/Friends and 16/16 Public grouped assertions. The PF
  run includes a finalized post-grant revisit; the Public run includes exact
  moderation, encounter, selection, interactions and withdrawal. Final runner
  JSON is being recaptured after the latest adversarial additions.
- The O55 pipeline now passes 10/10 with three R-GPS-01 seeds, discriminating
  noise-vs-excursion, switchback/hole, source-gap, Run pace/stop, repeated
  exploration, Finish and independent durable reload assertions. Evidence:
  `/Users/mzm/Desktop/cairn_revision03_work/evidence/raw-gps-pipeline-topology.json`.
- A fresh loaded Web run traversed ordinary Start through Finish/reload but was
  invalidated when source edits triggered Expo hot reload before theme capture;
  the partial output is retained and is not a PASS. Exact-source rerun remains
  the active Stage-A evidence task.

## Final-source validation checkpoint — 2026-09-19T17:20:00+08:00

- Personal/Friends disposable MySQL 8/API gate passes 19/19, including a real
  separate-connection authority lock: revoke waited 266 ms and the queued
  projection returned zero stale cells. Evidence: `pf-final-source/result.json`.
- Public disposable MySQL 8/API gate passes 17/17, including immutable review
  snapshots, the older-local-plus-60-newer-distant selection counterexample,
  and a 312 ms withdrawal lock schedule.
- Loaded O55 Web evidence passes all 20 stage/timing assertions under the
  frozen budgets. The final instrumentation measures handler acknowledgement
  inside the store transition (0.8 ms), Memory-to-source update (162.4 ms),
  and the next paint opportunity (16.9 ms), without Playwright polling delay.
- Representative geometry passes separately at 2,001+40 and 10,000 distinct
  locations with the 150 ms synchronous-slice budget unchanged. One-tile
  groups preserve the same exclusive 30 m footprint truth while avoiding a
  second Turf union.
- Scoped Activity core is 519/519 PASS; friend/Public cache, handler, account
  boundary is 55/55 PASS; focused server authority is 19/19 PASS.
- A connected loaded Public run exposed a normal Plant outbox defect: frontend
  fields crossed the strict marker API and caused HTTP 400. The shared marker
  outbox now whitelists its API payload. Failed connected attempts and their
  diagnostics remain retained; the real-API retry is active.

## Approved scope

1. Finish all revision-03 corrections and realistic GPS/Memory proof.
2. Freeze a separately recoverable Personal/Friends checkpoint.
3. Implement a feature-gated, text-only Public Cairn pilot with exact-revision
   moderation, qualified discovery, read-only detail, Thanks/Hide/Block/Report,
   and coherent withdrawal.
4. Test actual handlers, isolated API/MySQL, Web screens, races, performance,
   and a bounded endurance session.
5. Deploy backend code/schema only through the established runbook. Public is
   enabled only in the isolated review realm and remains server-disabled for
   general production use.
6. Prepare owner OTA instructions; never publish the OTA.

## Exclusions

No Public Routes, media/voice, comments/chat/follows/leaderboards, planner,
turn-by-turn, payments, App Store work, emergency service, physical-device or
field claims, general Public enablement, or automatic invitation.

## Dependency rule

Public may be planned and tested independently, but it may not be integrated
onto an unresolved friend authorization/cache foundation. Any source change
invalidates the dependent evidence until the affected gates are rerun.

## Compaction recovery

Reread this file, `04_WORK_QUEUE.json`, `03_ACCEPTANCE_PLAN.json`, and the open
items in `06_REVIEW_FINDINGS.md`; resume the single `in_progress` queue item.

## Primary recovery anchor — 2026-09-20

- Recovery HEAD: `0520b102eae78a1dd77aa39418a919d85f5648c1`.
- This records the existing bounded recovery point only; it does not classify
  any later candidate as integrated or passed.
