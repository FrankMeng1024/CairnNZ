# Revision 03 execution plan

1. Recover the current O60 filesystem baseline, contracts, review evidence, deployed identities, and reproduce C1–C4 through real modules/handlers.
2. Correct shared cache ordering/revision semantics, normal offline borrowed-Route use, bounded Encounter window selection, and prospective revisit projection.
3. Reuse the O55 calibrated Raw GPS generator through the normal normalization, Activity, live Memory, persistence, and renderer pipeline; run R-GPS-01 through R-GPS-07 with fixed seeds.
4. Measure correctness, live pre-Finish behavior, responsiveness, realistic accumulated scale, and loaded Expo Web Day/Sunset/Night evidence.
5. Run final affected/integrated gates, perform only required runbook-governed backend operations, freeze identities, and package a self-contained review archive without OTA publication.

Stop rule: deliver COMPLETE only if the software and required local validation gates pass; otherwise deliver a precise HOLD. Native device/physical-field-only claims remain explicitly separate.
