# Friends verdict

**READY for owner OTA approval; native review pending.**

Friends authorization is server-owned, prospective, asymmetric, non-transitive, and scoped per friend. The author enables future sharing; there is no permission request workflow. Derived Memory cells never include raw positions/times/order, and endpoint/private-place containment removes whole coarse cells.

Default denial, independent grants, new friendship epochs, revoke/unfriend/block/account-switch purge, 24-hour cache authority, rollback-resistant expiry, stale/out-of-order response denial, and old raw-endpoint containment are exercised in tests and the real MySQL/API harness.

Friend Cairns require both Friends object audience and a prospective server Encounter from accepted real canonical evidence. Viewers get an opaque read-only resource. Friends-visible Routes are also read-only and independent of Memory grants. Once an Activity legitimately starts, its borrowed geometry is an immutable, Activity-only safety snapshot that survives process recovery and is deleted at finish/discard; it cannot be exported, copied into ownership, or used to gain Memory.

FR-10's software/adversarial matrix passes, but its physical nearby portion is NOT_RUN. FR-14's Expo Web three-theme/map-unavailable subset passes, but native keyboard/safe-area/larger-text execution is NOT_RUN. This is not “device validation.”
